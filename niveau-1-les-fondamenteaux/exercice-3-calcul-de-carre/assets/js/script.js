// On crée notre fonction avec en argument nombre
//On retourne le carré (nombre x nombre)
// On crée une variable qui va stocker notre fonction et dans la fonction on met un nombre
//qui va etre utilisé pour faire le carré
// enfin, on affiche le résultat en console.log avec notre nouvelle variable

function calculerCarre(nombre) {
    return nombre * nombre
}

let result = calculerCarre(9)

console.log(result)